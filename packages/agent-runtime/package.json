{
  "name": "@cangjie/agent-runtime",
  "version": "0.1.0",
  "description": "Cangjie Agent Runtime — autonomous agent loop, tool system, context management",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@cangjie/shared": "workspace:*",
    "@anthropic-ai/sdk": "^0.39.0",
    "better-sqlite3": "^11.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "vitest": "^2.0.0"
  }
}
